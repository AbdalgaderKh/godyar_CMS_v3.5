
Generated package contains:
- Live Restore + Draft Restore
- SEO Overrides + Preview Center
- Safe Media Cleanup Queue

Validation:
- `php -l` passed on all added/updated PHP files.

Important:
- Live restore is best-effort and targets known `news` / `posts` table shapes only.
- Safe media cleanup archives to trash instead of hard deletion.
- SEO overrides are file-based in this package for compatibility and low risk.
