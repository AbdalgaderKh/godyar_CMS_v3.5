# More Suggestions After Phase 3

1. Add `ContactMessageRepository` and persist contact form submissions.
2. Add `AdminHealthController` to expose the same checks inside the admin panel.
3. Add JSON search suggestions endpoint for autosuggest.
4. Add `RedirectManager` CRUD in admin using the `redirects` table.
5. Add `TagRepository`, `AuthorRepository`, and revision history for articles/pages.
6. Add cache warm-up commands for homepage, sitemaps, and top categories.
7. Add image optimization pipeline and WebP fallback generation.
8. Add role-aware debug visibility so public users cannot access internal health pages in production.
